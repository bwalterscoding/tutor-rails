<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :student_name %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.number_field :grade, id: :student_grade %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_area :location, id: :student_location %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday, id: :student_birthday %>
  </div>

  <div class="field">
    <%= form.label :strength_weak %>
    <%= form.text_area :strength_weak, id: :student_strength_weak %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
